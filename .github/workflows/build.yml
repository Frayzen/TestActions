on: push
jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./
        shell: bash
    steps:
      - name: Check for example
        run: |
          chmod +x check.sh
          ./chech.sh
