on: push
jobs:
  check:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Simple ls
        run: |
          ls -la
          pwd
          tree
      - name: Check for example
        run: |
          chmod +x ./scripts/check.sh
          ./scripts/chech.sh
